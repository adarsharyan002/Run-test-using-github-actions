pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "hello deploy"
            }
        }

        stage('Test') {
            steps {
               echo "hello deploy"
            }
        }

        stage('Deploy') {
            steps {
                echo "hello deploy"
            }
        }
    }
}
